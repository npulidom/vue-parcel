/**
 * Plugins
 */

// Vue Transitions

[v-cloak] { visibility: hidden; }

.fade-enter-active {
	 transition: opacity .4s ease;
}
.fade-leave-active {
	transition: all .4s ease;
	opacity: 0.4;
}
.fade-enter, .fade-leave-to {
	 opacity: 0;
}
